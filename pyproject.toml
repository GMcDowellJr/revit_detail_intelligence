[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B"]  # errors, pyflakes, bugbear
ignore = []

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]